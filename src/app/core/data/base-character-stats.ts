import { CharacterStats } from '@core/interfaces/character-stats.interface';

/** Eleanora's unarmed base stats before equipment bonuses. */
export const BASE_CHARACTER_STATS: CharacterStats = {
  attack: 8,
  defense: 5,
  magic: 6,
  health: 100,
  luck: 4,
};
